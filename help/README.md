# Help topics (Marketer-Pro)

Short, linkable topics suitable for **in-app Help** or support macros. Expand as features ship.

| Topic | Summary |
|-------|---------|
| Connect Meta | Use OAuth; ensure Facebook app and redirect URI match environment. |
| Connect X | OAuth 2.0 PKCE; reconnect if token revoked. |
| Connect TikTok | OAuth + Content Posting scopes per TikTok program rules. |
| Connect LinkedIn | Member posting via approved LinkedIn app. |
| Scheduling | Slots map to `schedule_entries`; publish follows provider rules. |
| AI drafts | Disclosure and consent per store policy; drafts may be labeled as AI-assisted. |

_Add one small markdown file per topic when content stabilizes (e.g. `connect-meta.md`)._
