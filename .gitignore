# Dependencies
node_modules/

# Build outputs
dist/
build/
out/
.next/
.nuxt/
.turbo/
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example

# Logs & debug
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test & coverage
coverage/
.nyc_output/
*.lcov

# OS
.DS_Store
Thumbs.db
Desktop.ini

# IDE / editor (local)
.idea/
*.swp
*.swo
*~

# Cursor IDE — local caches, MCP descriptors, agent transcripts (Windows: long paths break `git add`)
.cursor/
!.cursor/rules/
!.cursor/rules/**

# Optional tooling caches
.eslintcache
.cache/
*.pem

# Nested full-repo clones under this monorepo (each has its own `.git/`).
# Tracking them produced broken submodule pointers (mode 160000) with no `.gitmodules`.
# Keep clones locally; canonical app code lives under `apps/`, `packages/`, `docs/`.
GitHub/
Wholesaler-Pro-b01-bootstrap-upgrades/

# Cursor plugin vendor clones (nested `.git`, huge; reinstall plugins if needed)
plugins/cache/

# Husky is tracked; leave hooks executable via git attributes if needed

# Claude Code — local memory & session files (machine-specific, never commit)
.claude/

# Database dumps — may contain real data
sqlite_dump.sql

# Cursor/agent autonomous-run artefacts (temp, root-level)
autonomous-run-events.ts
autonomous-run-events.test.ts
autonomous-run-events1.test.ts
autonomous-run-state.ts
