import { createStubProviderAdapter } from "./stub.js";

/**
 * X API v2 provider skeleton.
 *
 * Replace this stub with account credential lookup plus tweet/media upload
 * orchestration when live X publishing is enabled.
 */
export const xPublishProvider = createStubProviderAdapter("x");
