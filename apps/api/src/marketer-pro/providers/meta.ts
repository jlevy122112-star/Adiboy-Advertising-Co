import { createStubProviderAdapter } from "./stub.js";

/**
 * Meta Graph provider skeleton.
 *
 * Replace this stub with Page/Instagram credential lookup plus Graph API calls
 * once live social connection secrets are available to the API process.
 */
export const metaPublishProvider = createStubProviderAdapter("meta");
