import { createStubProviderAdapter } from "./stub.js";

/**
 * TikTok provider skeleton.
 *
 * Replace this stub with partner API credential lookup plus video/photo
 * publishing once TikTok app review and scopes are in place.
 */
export const tiktokPublishProvider = createStubProviderAdapter("tiktok");
