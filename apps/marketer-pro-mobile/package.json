{
  "name": "@home-link/marketer-pro-mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:web": "npm run build -w apps/web --prefix ../..",
    "sync": "npx cap sync",
    "sync:ios": "npx cap sync ios",
    "sync:android": "npx cap sync android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "build:ios": "npm run build:web && npx cap sync ios",
    "build:android": "npm run build:web && npx cap sync android",
    "run:ios": "npm run build:web && npx cap run ios",
    "run:android": "npm run build:web && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
